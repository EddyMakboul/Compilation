PROGRAMME exemple;
CONST testConst=52, otherConst='test de constante 2', test='test d''erreur';
VAR nb, var2, var3;
DEBUT
    { Test de commentaire }
    ECRIRE('Entrez un nombre entier:');
    LIRE(nb, var2);
    nb:=(1*testConst);
    ECRIRE('test d''ecriture');
    LIRE(nb);
FIN
